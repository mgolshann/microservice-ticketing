import { Topics } from '../topics';
export interface TicketCreatedEvent {
    topic: Topics;
    data: {
        id: string;
        title: string;
        price: number;
        userId: string;
    };
}
