import { Topics } from '../topics';
export interface TicketUpdatedEvent {
    topic: Topics;
    data: {
        id: string;
        title: string;
        price: number;
        userId: string;
    };
}
